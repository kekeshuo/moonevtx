# Contributing

Keep changes format-specific. Prefer a new substitution type or a writer option over a Windows API binding.

```text
python scripts/verify.py
```

Do not add captured `.evtx` files from third-party hosts. Generate fixtures with `write_events`.
