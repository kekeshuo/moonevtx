name = "kekeshuo/moonevtx"

version = "0.1.0"

readme = "README.md"

license = "Apache-2.0"

keywords = [ "evtx", "binxml", "windows-event-log", "forensics", "python-evtx" ]

preferred_target = "wasm-gc"

description = "Windows EVTX and BINXML codec with CRC verification, XML rendering and an in-memory writer"

repository = "https://github.com/kekeshuo/moonevtx"
